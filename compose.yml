services:
  go-dev:
    image: golang:1.24
    container_name: dev-container
    volumes:
      - ./src:/app
    working_dir: /app
    environment:
      - GO111MODULE=on
      - GOPROXY=https://proxy.golang.org,direct
    command: tail -f /dev/null

volumes:
  go-mod-cache: